/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['source.unsplash.com', 'shop.chaipoint.com', 'lh3.googleusercontent.com']
    }
};

export default nextConfig;
